image: alpine:latest

pages:
  stage: deploy
  script:
    # If files aren't in a folder named 'public' yet, create it and move them there
    - mkdir -p public
    - cp -r * public/ || true
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
